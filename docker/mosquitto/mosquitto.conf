# Development Mosquitto config.
# NOTE: anonymous access is enabled for local dev only. For staging/production,
# disable anonymous and configure a password file / TLS listener.

listener 1883
allow_anonymous true

listener 9001
protocol websockets

persistence true
persistence_location /mosquitto/data/
log_dest stdout
